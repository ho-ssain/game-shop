/* eslint-disable no-unused-vars */
/* eslint-disable react/prop-types */
import "./Categories.css";

function Categories({ games, reference }) {
  return (
    <section id="catg" className="catg" ref={reference}>
      <h1> Categories </h1>
    </section>
  );
}

export default Categories;
